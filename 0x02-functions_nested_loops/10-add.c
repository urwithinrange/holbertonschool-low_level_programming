#include "holberton.h"
/**
  * add -  function that adds two integers and returns the result
  *@a: first number passes
  *@b: second number passed
  * Return: the result of a + b
  */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
